package src.main.java.modules.throttle;

/*import org.springframework.web.bind.annotation.*;
import src.main.java.services.ThrottleService;
import src.main.java.services.ThrottleServiceImpl;

@RestController
@RequestMapping("/api/throttle")
public class throttleController {

    private final ThrottleService throttleService = new ThrottleServiceImpl();

    @PostMapping("/limit")
    public String limitThrottle() {
        throttleService.limitThrottle();
        return "Throttle limited.";
    }

    @PostMapping("/release")
    public String releaseThrottle() {
        throttleService.releaseThrottle();
        return "Throttle released.";
    }
}*/

